import { TeamplayersPipe } from './teamplayers.pipe';

describe('TeamplayersPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamplayersPipe();
    expect(pipe).toBeTruthy();
  });
});

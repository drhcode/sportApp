import { BestPlayerPipe } from './best-player.pipe';

describe('BestPlayerPipe', () => {
  it('create an instance', () => {
    const pipe = new BestPlayerPipe();
    expect(pipe).toBeTruthy();
  });
});
